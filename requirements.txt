# Core pipeline
librosa>=0.10,<2.0        # audio loading + feature extraction
soundfile>=0.12           # librosa's WAV backend
numpy>=1.24
pandas>=2.0
joblib>=1.3               # saving/loading trained pipelines
natsort>=8.0              # natural sort for file listings

# scikit-learn is pinned below 1.10 on purpose.
#   LogisticRegression(penalty=...)  is removed in 1.10
#   SVC(probability=True)            is removed in 1.11
# Both are used with the paper's exact hyperparameters. Lifting this pin
# requires migrating to l1_ratio= and CalibratedClassifierCV.
scikit-learn>=1.3,<1.10

# Plotting (optional -- only for figures)
matplotlib>=3.7

# Dataset download (optional -- only for scripts/download_dataset.py)
kagglehub>=0.3
